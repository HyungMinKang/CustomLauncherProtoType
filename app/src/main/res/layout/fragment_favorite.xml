<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- 즐겨찾는 앱 타이틀 -->
    <TextView
        android:id="@+id/appSectionTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="즐겨찾는 앱"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- 즐겨찾는 앱 그리드 -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/favoriteAppRecyclerView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:focusable="true"
        android:nestedScrollingEnabled="false"
        app:layout_constraintTop_toBottomOf="@id/appSectionTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp"/>

    <!-- 즐겨찾는 콘텐츠 타이틀 -->
    <TextView
        android:id="@+id/contentSectionTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="즐겨찾는 콘텐츠"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/favoriteAppRecyclerView"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- 즐겨찾는 콘텐츠 수평 리스트 -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/favoriteContentRecyclerView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:focusable="true"
        android:nestedScrollingEnabled="false"
        app:layout_constraintTop_toBottomOf="@id/contentSectionTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp" />
</androidx.constraintlayout.widget.ConstraintLayout>
